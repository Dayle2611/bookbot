def get_num_words(stringText):
    wordList = stringText.split()
    wordCount = len(wordList) 
    return wordCount

def get_unique_chars(stringText):
    unique = set(stringText)
    return unique

